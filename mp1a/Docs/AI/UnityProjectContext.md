# MP1A project context

- Vampire props replace the former key HUD/inventory. `Assets/VampireProps/VampireRelics.prefab` contains three physical XRGrabInteractable subclasses and three matching XRSocketInteractor subclasses: blood vial/chalice, silver stake/coffin, bat medallion/crypt gate. Each has a distinct silhouette, label, color and opening mechanism. SampleScene places the group at (0,0,3). Keys are separate nested prefabs on front pedestals (world Z=1.2); independent lock prefabs are behind them (world Z=3.6). All three keys and three loose stones use gravity; the existing floor MeshCollider remains enabled.
- `LeftDirectGrab.prefab` and `RightDirectGrab.prefab` are added under the corresponding XR controller through scene overrides. `VampireDirectInteractor` joins its parent XRInteractionGroup with direct-grab priority and reuses XRI Select grip actions. No new input asset or project setting changes. Isolated Unity Play Mode verified physics, direct hover/grab/release, hand-group membership, all wrong-pair rejections, all three openings and one-shot installed-key behavior. Full main-scene headset comfort is not validated. See `Docs/VampireRelics.md`.


- Unity 6000.5.6f1; project root is `mp1a_group8/mp1a`.
- URP 17.5, Input System 1.20, OpenXR 1.17.1, XR Interaction Toolkit 3.5.1.
- The enabled build scene is `Assets/Scenes/SampleScene.unity`.
- Scene composition is primarily serialized GameObjects and small root-level MonoBehaviours. Imported XR samples have their own assemblies; first-party scripts use the default assembly.
- The room spans x/z -7.5 to 7.5, floor y=0, ceiling y=15. The planet is at (0,7.5,0); the XR rig starts at the origin. The primitive car decoration is at (4.69,0,-4.883), rotated 90 degrees around Y.
- `planetOrbit`, `CometOrbit`, `breakout`, `light`, and `quit` own the existing interactions. Preserve their scene wiring.
- Unity Test Framework is installed; no first-party test assemblies were found. No Unity MCP tools or project bridge were detected. Editor log is `Logs/Editor.log`; live scene/play-mode validation is unavailable through MCP.
- Parking decorations are static geometry in `Assets/Decorations/ParkingLot.prefab`, instantiated once in SampleScene. They use shared URP materials and built-in meshes, with no new scripts, packages, lights, physics, or input behavior. The original floor still owns collision.
- Blood moon feedback is controlled by `BloodMoonEffects` on Point Light. Three cloud ParticleSystems are serialized as children of planet, moon, and comet in SampleScene; seven burst ParticleSystems are serialized under the four trees and three people in ParkingLot.prefab. All module settings, materials, and emission counts live in those assets. The script only starts/stops assigned ParticleSystem arrays. No runtime particle creation or tree lights.

- Additional hierarchy feedback: two lamp spark children and two cottage window mist children join the blood moon burst array. Cottage Doorway Fog is assigned to BreakOut.viewChangeParticle and plays on view switching. Total input-triggered ParticleSystems: 15.
- Spatial audio: 13 saved 3D AudioSources (6 celestial-body cues, 3 gasps in ParkingLot, 2 teleport cues, welcome, farewell). SpatialSoundFeedback on Point Light is called by LightSwitch, BreakOut and Quit. Imported audio is under Assets/sound_effect; used clips are mono/preloaded. Quit waits for the farewell duration. Isolated Unity tests verified native clip imports, 3D settings, playback routing, welcome once, teleport placement, farewell duration; perceptual mixing in the main scene is still a manual check.

- Shooting was removed: spawner and movement scripts, four shooting scene objects, shot-triggered car horn children, fireball/car_beep/horn clips and the controls label. Shared input actions and materials remain.

- Car portability: Assets/Decorations/PrimitiveCar.prefab replaces the imported car. All 30 visible parts use built-in Unity cube/cylinder meshes and existing URP decoration materials, with one static BoxCollider and no scripts. The old car.prefab and de-poly-car.blend source were removed. Isolated Unity validation reopens the prefab and verifies mesh/material references and collider count; its preview uses equivalent built-in shading, not the main scene URP pipeline.

- Ray grabbing: the existing right NearFarInteractor uses the XRI Right Interaction Select grip action. VampireKey now gives gold hover and cyan held feedback for ray and direct interactors. All three key prefabs set Far Attach Mode=Near, bringing a ray-selected key to the hand. Independent assets: BloodVialKey, SilverStakeKey, BatMedallionKey, ChaliceLock, CoffinLock, CryptLock. Play Mode checks with the actual right NearFar prefab, XR Origin, hand interaction group, and simulated right-controller grip events passed for all three target/grab/carry/release/unlock paths. Simulator right-hand selection is ], grip is G.

- Escape round: VampireEscape on VampireRelics creates an in-world goal/progress sign and requires three distinct solved locks with installed matching keys and completed opening animations before one OnEscaped event. The win sign leaves the scene playable for recording. VampireLock now eases its indicator to green over the same 1.1 seconds as its mechanism. Tabletop box triggers accept matching released props over the full lock shelf. Simulator Point and Click left mouse also simulates grip.

- Text presentation: all nine key-name/lock-title/status labels are saved opaque TextMeshPro 3D components with an outlined font material. Lock titles are raised to avoid the foreground pedestals. General explanations and the live escape counter are four TextMeshProUGUI children of the saved world-space Escape Instructions Canvas. VampireEscape references those labels instead of creating a runtime sign. VampireLock statusLabel now references TMP_Text. Unity imported the converted prefabs and passed isolated Play Mode status/progress/victory checks; URP previews reviewed.

- Canvas correction: instructions/progress now belong to the original SampleScene object Canvas (existing back-wall transform retained), together with its original Right Primary/Quit, Right Secondary/Light Color, Left Primary/View controls. Removed Escape Instructions Canvas from VampireRelics. Heading/progress references are scene prefab overrides pointing to the original Canvas's TMP children; table labels are unchanged.

- Boarded wall reveal: BoardedCryptDoor inside VampireRelics waits for Blood Tithe and Hunter's Seal (either order), then eases four nailed boards away and swings an opaque oak door inward. A passage collider remains until the animation completes. Night Crest and its bat-key pedestal are now inside an enclosed 5.6 x 6.4 m crypt beyond world Z=7.5, with solid floor/walls/ceiling and two warm lights. Their interactions stay disabled until revealed. Original front_plane is inactive; three solid wall sections replace it around a 2.4 x 2.5 m doorway. Their front faces remain at Z=7.5, behind the existing Canvas at Z=7.49. The Canvas explains the two-lock reveal. Main room's first two lock locations are unchanged. No teleport gate was added in this step.

- Teleport-door correction: boards clear after the first two locks, but both door leaves and passage collision stay closed. CryptDoorTeleport on the crypt group reuses ControllerSampleActions LeftHand/SecondaryButton. Scene overrides reference the real XROrigin and left controller; aiming uses the left NearFarInteractor's effective cast origin. A clear front-facing ray within 10 m plus a fresh button press teleports to the crypt or main-room marker, preserving camera height and setting the destination facing direction. Return door is a separate inside-facing leaf. Existing Canvas retains original controls plus Left Secondary (Shift + 2) / Aim at Door + Teleport.

- Five gates: existing crypt plus house, blue-car rear door, green-car rear door, and parking-bay planet portal. House/Blood Tithe and blue-car/coffin now live in separate enclosed rooms under VampireRelics, preserving the original three lock references. Green car is a native PrimitiveCar nested instance with saved green material overrides; its cabin is ordinary seating. Silver stake moved onto a solid terrace at world (0,8.5,0), above the original rotating planet; return portal carries held relics safely. CryptDoorTeleport owns all eight new directional gate entries plus the existing crypt pair, uses the same left Secondary input and animates car/return doors before travel. Canvas route text updated; original controls retained. Main project C# compile and isolated runtime-clone Play Mode tests passed (simulated left/right secondary input, all new round trips, held-key travel/release, reveal and victory). Reviewed saved URP previews. No project settings or original blue-car/house prefab changes in this task.
